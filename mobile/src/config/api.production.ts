/**
 * Deployed web app API — update when you publish the Laravel backend.
 */
export const PRODUCTION_API_CONFIG = {
    baseUrl: 'https://your-production-domain.com',
    label: 'Production',
} as const;
